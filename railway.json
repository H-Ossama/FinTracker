{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && npm ci --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma db push && npx prisma generate && npm start",
    "restartPolicyMaxRetries": 5
  }
}
